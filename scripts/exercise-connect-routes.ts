/**
 * NOTE: This file is NOT used as verification evidence.
 * Real HTTP route exercise + post-state merged availability is captured in verif-real-route-flow.txt
 * and stage5-mcp.log using actual curl against running Next dev server + mcp.
 */
console.log("Real evidence in verif-*.txt and stage5-*.log (see full-verif-recheck etc)");
